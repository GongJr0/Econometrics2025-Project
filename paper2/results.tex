\section{Results}\label{sec:results}
\subsection{Data Examination}\label{subsubsec:design_matrix}
With the design matrix constructed as per Section~\ref{subsubsec:inp_mat}, we can compute the inter-regressor interactions with VIF and the regressor-target relations with CCF.
Our initial VIF computations are as follows:
\begin{table}[H]
    \centering
    \begin{tabular}{lc}
        \toprule
        \textbf{Regressor} & \textbf{VIF} \\
        \midrule
        $pce$ & 4.79 \\
        $pdi$ & 2.05 \\
        $govexp$ & 1.53 \\
        $ugap$ & 9.22 \\
        $ogap$ & 3.94 \\
        $okun$ & 4.62 \\
        $phillips$ & 3.22 \\
        \bottomrule
    \end{tabular}
    \caption{Variance Inflation Factor (VIF) of all regressors.}
    \label{tab:vif_results}
\end{table}

All VIF values are below the threshold, and we can conclude that multicollinearity is not a concern in our design matrix.


\begin{figure}[H]
\label{fig:ccf}
    \centering
    \includegraphics[width=0.75\textwidth]{fig/ccf}
    \caption{CCF plots of the whitened exogenous variables and the dependent variable.}
\end{figure}

The CCF results in Figure~\ref{fig:ccf} show all regressors have significant lag-1 correlations, supporting the validity of the matrix specification with $L^1$.
As expected, $pce$ and $pdi$ show strong positive correlations while $ugap$ shows a negative correlation as theorized.
Moreover, almost all variables with significant lags have a sign changed significant correlation immediately after the initial lag that's significant.
This might be an indicator of cyclical behavior, or \enquote{response functions} with overshooting.
$ogap$ and $okun$ show particularly similar magnitudes and sign patterns.
With $ogap$ being a component of $okun$, we can hypothesize that $okun$'s correlation pattern is mostly inherited from $ogap$ rather than $ugap$. (at least in the first 2 lags)



\subsection{Model Estimation}\label{subsec:model_estimation}

Having confirmed the design matrix is sufficiently well-behaved, we proceed with the estimation of the initial model.
The fit results in the coefficients:
\begin{table}[H]
    \centering
    \begin{tabular}{lccc}
        \toprule
        \textbf{Variable} & \textbf{Coefficient} & \textbf{t-test $p$} & \textbf{Significance} \\
        \midrule
        $C$ & 0.0288 & 0.8309 & $\text{\sffamily{X}}$ \\ 
        $pce$ & 76.8300 & $\approxeq 0$ & $\checkmark$ \\
        $pdi$ & 9.7348 & 0.0229 & $\checkmark$ \\
        $govexp$ & -22.6115 & 0.0730 & $\text{\sffamily{X}}$ \\
        $ugap$ & 1.1538 & $\approxeq 0$ & $\checkmark$ \\
        $ogap$ & -5.0182 & 0.5201 & $\text{\sffamily{X}}$ \\
        $okun$ &  -3.4977 & 0.0670 & $\text{\sffamily{X}}$ \\
        $phillips$ & -32.7522 & 0.0315 & $\checkmark$ \\
        \midrule
        & & & $\sum_\checkmark = 4$ \\
        \bottomrule
    \end{tabular}
    \caption{Estimated Coefficients of the Initial Model.}
    \label{tab:estimated_coefficients}
\end{table}

Table~\ref{tab:estimated_coefficients} shows a decent result with multiple coefficients being significant. 
We see that one of our interaction terms, $phillips$, has an individually significant coefficient while $okun$ does not.
The overall model fit statistics confirm this observation:
\begin{table}[H]
    \centering
    \begin{tabular}{cc}
        \toprule
        \textbf{Statistic} & \textbf{Value} \\
        \midrule
        $R^2$ & 0.4071 \\
        Adjusted $R^2$ & 0.3703 \\
        RMSE & 0.864 \\
        F-statistic & 36.8205 \\
        p-value ($F$) & $\approxeq 0$ \\
        $X^\top \hat{\varepsilon}$ & $-8.49 \times 10^{-14} \approxeq 0$ \\
        \bottomrule
    \end{tabular}
    \caption{Overall model fit statistics.}
    \label{tab:model_fit_statistics}
\end{table}
\begin{figure}[H]
        \centering
        \includegraphics[width=0.55\textwidth]{fig/fit_v_obs}
        \caption{Fitted vs. Observed Values}
        \label{fig:fitted_vs_observed}
\end{figure}

\noindent With an $R^2$ of 40.71\% and a passing $F$-test, the fit looks good at face value.
However, the application of a RESET test tells a different story. 
The current model produces a $p$-value of $2.34\times 10^{-14}$ which is a strong indicator of misspecification.
In fact, Figure~\ref{fig:fitted_vs_observed} hints at a possible cause of this.
With the 2008 Financial Crisis and 2020 COVID shocks in the data, we clearly see that the model goes out of its way to fit said points.
This is especially visible in 2020Q3 where the model re-produces the extreme positive shock to output growth.
This behavior is not expected from a generalized model and is potentially working against the model specification test.
To confirm our suspicions of extreme outliers, we plot the Cook's Distance of all residuals:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{fig/cooks_d.png}
    \caption{Cook's Distance of Initial Model Residuals.}
    \label{fig:cooks_distance}
\end{figure}

\noindent We immediately see that the COVID-19 shocks dominate the influence statistics.
$D_{2020Q3} = 5.81$ is orders of magnitude above the $4/n \approxeq 0.03$ threshold.
In total, 10 observations exceed the threshold. 
3 of these are attributable to the Financial Crisis (between 2008Q4 and 2010Q1) while the remaining 7 are consecutive observations from near-COVID (2020Q2--2021Q4).

\vspace{\baselineskip}

In light of these results, it is clear that COVID-19 brings about undesirable fit-time behavior into the model.
Of course, these observations are not to be discarded lightly, especially given time-continuity is crucial for preserving the interpretability of lagged models.
However, given 7 consecutive observations with extreme influence on the model, there's a possibility of structural breaks.
Therefore, two Chow tests were conducted with the break points at 2020Q2 and 2020Q3.
Both tests confirmed the presence of structural breaks with $p_{2020Q2} = 0.01$ and $P_{2020Q3}\approxeq 0$.
Seeing the results, we elected to re-estimate the current model using pre-COVID data before running further tests.

\subsection{Pre-COVID Re-Estimation}\label{subsec:pre_covid_reestimation}
After confirming the VIF of the pre-COVID design matrix cleared the thresholds ($VIF(X_i) < 10 \, \forall X_i \in X$), the model was re-estimated and the following coefficients were obtained:
\begin{table}[H]
    \centering
    \begin{tabular}{lccc}
        \toprule
        \textbf{Variable} & \textbf{Coefficient} & \textbf{t-test $p$} & \textbf{Significance} \\
        \midrule
        $C$ & 0.1934 & 0.0528 & $\text{\sffamily{X}}$ \\ 
        $pce$ & 63.5334 & $\approxeq 0$ & $\checkmark$ \\
        $pdi$ & 4.5738 & 0.0190 & $\checkmark$ \\
        $govexp$ & -10.7911 & 0.1622 & $\text{\sffamily{X}}$ \\
        $ugap$ & -0.1312 & 0.5661 & $\text{\sffamily{X}}$ \\
        $ogap$ & -13.8038 & 0.0043 & $\checkmark$ \\
        $okun$ &  -0.1075 & 0.9322 & $\text{\sffamily{X}}$ \\
        $phillips$ & -32.7953 & 0.0015 & $\checkmark$ \\
        \midrule
        & & & $\sum_\checkmark = 4$ \\
        \bottomrule
    \end{tabular}
    \caption{Estimated Coefficients of the Pre-COVID Model.}
    \label{tab:precovid_estimated_coefficients}
\end{table}

As in the previous estimation, Table~\ref{tab:precovid_estimated_coefficients} show four individually significant coefficients.
Notably, $ogap$ becomes significant while $ugap$ loses its significance.
The overall model fit statistics are as follows:
\begin{table}[H]
    \centering
    \begin{tabular}{cc}
        \toprule
        \textbf{Statistic} & \textbf{Value} \\
        \midrule
        $R^2$ & 0.3955 \\
        Adjusted $R^2$ & 0.3507 \\
        RMSE & 0.4525 \\
        F-statistic & 8.83 \\
        p-value ($F$) & $\approxeq 0$ \\
        $X^\top \hat{\varepsilon}$ & $-5.30 \times 10^{-14} \approxeq 0$ \\
        \bottomrule
    \end{tabular}
    \caption{Overall model fit statistics.}
    \label{tab:precovid_model_fit_statistics}
\end{table}
\begin{figure}[H]
        \centering
        \includegraphics[width=0.55\textwidth]{fig/precov_fit_v_obs.png}
        \caption{Fitted vs. Observed Values (Pre-COVID Model)}
        \label{fig:precovid_fitted_vs_observed}
\end{figure}
The fit statistics in Table~\ref{tab:precovid_model_fit_statistics} are comparable to the initial estimation, with a slight decrease in $R^2$.
However, Figure~\ref{fig:precovid_fitted_vs_observed} again displays the regime-breaking 2008 shock. 
Knowing the model specification issues of the previous fit, we confirm $D_{2008Q4}$ is well above the $4/n$ threshold before running the RESET test again.
The test again results in a rejection with $p = 0.0167$.
Although this is again a misspecification indicator, we observe that a sizable improvement in $p$-value has been achieved without explicitly adjusting any regressors.
However, the strategy of pulling back the observations in this case would lead to a large loss of data.
After evaluating the 2008Q4 point as a break point candidate in a Chow test, we found $p=0.1427$ and could not confirm a break.
Therefore, we proceed with the pre-COVID model while introducing a signal dummy $I_{2008Q4} = \mathbbm{1}_{\lbrace t=2008Q4 \rbrace}$.
By this approach, we're effectively treating 2008Q4 as a single-period shock.
The dummy provides the model with a single coefficient to deal with the 2008 shock exactly while having no effect on other observations.
On the other hand, this choice introduces a loss in degrees of freedom (DoF) by increasing the $X$ dimension.

\subsection{Shock Adjusted Model Re-Estimation}\label{subsec:shock_adjusted_reestimation}
For one more time, we confirm the VIF of all regressors (including the newly added dummy) are below threshold and re-estimate the model to see if the loss of DoF is justified.
The estimation results in the coefficients:
\begin{table}[H]
    \centering
    \begin{tabular}{lccc}
        \toprule
        \textbf{Variable} & \textbf{Coefficient} & \textbf{t-test $p$} & \textbf{Significance} \\
        \midrule
        $C$ & 0.2714 & 0.0059 & $\checkmark$ \\ 
        $pce$ & 54.3276 & $\approxeq 0$ & $\checkmark$ \\
        $pdi$ & 4.8823 & 0.0088 & $\checkmark$ \\
        $govexp$ & -9.5442 & 0.1942 & $\text{\sffamily{X}}$ \\
        $ugap$ & 0.0241 & 0.9135 & $\text{\sffamily{X}}$ \\
        $ogap$ & -10.5652 & 0.0234 & $\checkmark$ \\
        $okun$ &  0.2778 & 0.8179 & $\text{\sffamily{X}}$ \\
        $phillips$ & -24.2331 & 0.0157 & $\checkmark$ \\
        $I_{2008Q4}$ & -1.7381 & 0.0006 & $\checkmark$ \\
        \midrule
        & & & $\sum_\checkmark = 6$ \\
        \bottomrule
    \end{tabular}
    \caption{Estimated Coefficients of the Pre-COVID Model.}
    \label{tab:shockadj_estimated_coefficients}
\end{table}

Table~\ref{tab:shockadj_estimated_coefficients} shows that the added dummy term is highly significant.
Moreover, the addition has caused the intercept to become individually significant.
The overall model fit statistics are as follows:
\begin{table}[H]
    \centering
    \begin{tabular}{cc}
        \toprule
        \textbf{Statistic} & \textbf{Value} \\
        \midrule
        $R^2$ & 0.4577 \\
        Adjusted $R^2$ & 0.4121 \\
        RMSE & 0.4286 \\
        F-statistic & 10.0332 \\
        p-value ($F$) & $\approxeq 0$ \\
        $X^\top \hat{\varepsilon}$ & $2.00 \times 10^{-14} \approxeq 0$ \\
        \bottomrule
    \end{tabular}
    \caption{Shock Adjusted model fit statistics.}
    \label{tab:shockadj_model_fit_statistics}
\end{table}
\begin{figure}[H]
        \centering
        \includegraphics[width=0.55\textwidth]{fig/shockadj_fit_v_obs.png}
        \caption{Fitted vs. Observed Values (Shock Adjusted Model)}
        \label{fig:shockadj_fitted_vs_observed}
\end{figure}

The fit statistics in Table~\ref{tab:shockadj_model_fit_statistics} show a sizable improvement in $R^2$ alongside an increase in the $F$-statistic.
With the extravagant shock now accounted for, Figure~\ref{fig:shockadj_fitted_vs_observed} looks much better in terms of the presence of high-influence outliers.
Plotting the Cook's Distance confirms this:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{fig/shockadj_cooks_d.png}
    \caption{Cook's Distance of Shock Adjusted Model Residuals.}
    \label{fig:shockadj_cooks_distance}
\end{figure}
\noindent Although there still are 8 points above threshold, the distribution of $D_i$ looks much more uniform and sensible.
Running the RESET test one final time yields a $p$-value of $0.3837$; indicating no evidence against correct model specification.
With this confirmation, we proceed our analysis with the shock adjusted model.

\subsection{Residual Diagnostics}\label{subsubsec:residual_diagnostics}
Although the Cook's Distance plot displayed a basic look at the residuals, further diagnostics and tests are necessary to reach an accurate evaluation of the fit.
Normality, Homoskedasticity, Autocorrelation, and Stationarity of residuals will be examined in this section.

\subsubsection{Normality}\label{subsubsec:residual_normality}
Normality can both be tested for and examined visually.
A QQ Plot alonside a Shapiro--Wilk test will be employed for this purpose.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{fig/shockadj_qq.png}
    \caption{QQ Plot of Shock Adjusted Model Residuals.}
    \label{fig:shockadj_qq}
\end{figure}

The QQ Plot against normal quantiles shows that all residuals are contained within the 95\% CI bands.
This is a strong indicator of normality, which is further confirmed by the Shapiro--Wilk test results of $SW = 0.9825$ and $p=0.1321$ failing to reject the null hypothesis of normality.

\subsubsection{Homoskedasticity}\label{subsubsec:residual_homoskedasticity}
Homoskedasticity is an important indicator of spherical errors and can be tested using the Breusch--Pagan test.
A BP application on the shock adjusted model residuals yields $BP = 7.21$ and $p=0.4853$; failing to reject the null hypothesis of homoskedasticity.

\subsubsection{Autocorrelation}\label{subsubsec:residual_autocorrelation}
Autocorrelation of residuals indicates slow-moving patterns in the errors, which is undesirable and often results in inefficient estimates.
To test for autocorrelation, we apply the Breusch--Godfrey test for lags $[1,8]$ (2 years).

\begin{table}[H]
    \centering
    \begin{tabular}{lcccccccc}
        \toprule
        \textbf{Lag} & 1 & 2 & 3 & 4 & 5 & 6 & 7 & 8 \\
        \midrule
        \textbf{$BG$} & 5.00 & 5.57 & 6.93 & 8.57 & 8.67 & 9.84 & 10.19 & 10.77 \\
        \textbf{$p$} & 0.975 & 0.938 & 0.926 & 0.927 & 0.877 & 0.868 & 0.822 & 0.785 \\
        \bottomrule
    \end{tabular}
    \caption{Breusch--Godfrey Test Results for Shock Adjusted Model Residuals.}
    \label{tab:shockadj_bg_results}
\end{table}
\noindent All lags fail to reject the null hypothesis of no autocorrelation with strong $p$-values as seen in Table~\ref{tab:shockadj_bg_results}.

\subsubsection{Stationarity}\label{subsubsec:residual_stationarity}
Finally, stationarity of residuals is an important aspect to consider in time series models.
Non-stationary residuals can indicate omitted trends or cycles in the model specification.
To test for stationarity, we apply the Augmented Dickey--Fuller (ADF) test on the residuals.
The ADF test results in $ADF = -2.91$ and $p=0.0036$; rejecting the null hypothesis of a unit root and indicating stationarity of residuals.

\subsection{Gauss\rule[0.5ex]{0.5em}{0.7pt}Markov Conditions}\label{subsubsec:gm_conditions}

Although we present strong evidence towards all Gauss-Markov conditions being satisfied, we further confirm our findings using the tests outlined in Section~\ref{subsec:blue_evaluation}.

\begin{itemize}
    \item \textbf{Linearity:} The model is linear in parameters by design (Equation~\ref{eq:model_eq}).
    \item \textbf{Full Rank:} We computed a lack of perfect multicollinearity with $VIF(X_i) < 5 \,\, \forall X_i \in X$ and $\operatorname{cond}(X) = 75.64$.
    While a $cond(X) < 30$ is the commonly used diagnostic threshold, having multiple interactions in $X$ increases the expected condition number by design.
    Given the low VIF values, we accept the fulfillment of the full rank condition.
    \item \textbf{Spherical Errors:} We found no evidence against homoskedasticity, no autocorrelation up to lag 8, and stationarity of residuals.
    Therefore, we accept the fulfillment of the spherical errors condition.
    \item \textbf{Strict Exogeneity:} While strict exogeneity is not testable, the model specification tests and residual diagnostics were all in favor of correct model specification.
    While said tests are definitively not sufficient for the strong claim of exogeneity, we believe that the evidence presented is supporting weak exogeneity.
    Although further work would be necessary to confirm this, we will not deny the possibility of BLUE on the basis of this uncertainty.
    
\end{itemize}